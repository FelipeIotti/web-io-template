import { Loading } from ".";

export function LoadingPage() {
  return (
    <div className="flex  h-[100vh] w-full items-center justify-center">
      <Loading className="h-full w-2xl" />
    </div>
  );
}
